import { RetouchePage } from "@/components/retouche/RetouchePage";

export const metadata = {
  title: "Retouche Vinted — Pictaura",
  description: "Vendez plus vite avec des photos marketplace professionnelles.",
};

export default function VintedPage() {
  return <RetouchePage agentKey="VINTED" />;
}
